import React from 'react';
import PropTypes from 'prop-types';

const UseCallback = props => {
  return (
    <div>
      <h1> Đây là trang học useCallback !</h1>
    </div>
  );
};

UseCallback.propTypes = {

};

export default UseCallback;